// Write a JS program to toggle case of  each character of a string.

let toggle = (string) => {
  let sep = string.split(0, 3);
  return sep;
};

let str = "Love YourSelf First";
console.log(toggle(str));
