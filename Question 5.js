// WRite a JS program to convert  lowercase string to uppercase.
//  used Expression function

let UpperCase = function (string) {
  return string.toUpperCase();
};
let str = "Helo! i am Amna FRom Pakistan...";
console.log(UpperCase(str));
